import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { formatPct } from '../utils/formatters.js';
export default function AllocationChart({ breakdown={}, targetAllocation={} }){ const data=Object.keys(targetAllocation).map(c=>{const actual=breakdown[c]?.weight||0;return {category:c,目标:targetAllocation[c],实际:actual,dev:Math.abs(actual-targetAllocation[c])}}); const color=d=>d<.03?'#22c55e':d<.08?'#eab308':'#ef4444'; return <div className="grid gap-4 lg:grid-cols-3"><div className="card p-4 lg:col-span-2"><h3 className="mb-3 font-semibold">目标 vs 实际配置</h3><ResponsiveContainer width="100%" height={260}><BarChart data={data}><CartesianGrid stroke="#3e3e42"/><XAxis dataKey="category"/><YAxis tickFormatter={(v)=>formatPct(v)}/><Tooltip formatter={(v)=>formatPct(v)}/><Legend/><Bar dataKey="目标" fill="#007acc"/><Bar dataKey="实际">{data.map(d=><Cell key={d.category} fill={color(d.dev)}/>)}</Bar></BarChart></ResponsiveContainer></div><div className="card p-4"><h3 className="mb-3 font-semibold">实际占比</h3><ResponsiveContainer width="100%" height={260}><PieChart><Pie data={data} dataKey="实际" nameKey="category" outerRadius={90} label={({category,实际})=>`${category} ${formatPct(实际)}`}>{data.map((d,i)=><Cell key={d.category} fill={['#007acc','#4ade80','#facc15','#f97316'][i%4]}/>)}</Pie><Tooltip formatter={(v)=>formatPct(v)}/></PieChart></ResponsiveContainer></div></div> }
