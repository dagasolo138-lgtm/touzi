import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { formatPct } from '../utils/formatters.js';
export default function AllocationChart({ breakdown={}, targetAllocation={} }){ const data=Object.keys(targetAllocation).map(c=>{const actual=breakdown[c]?.weight||0;return {category:c,目标:targetAllocation[c],实际:actual,dev:Math.abs(actual-targetAllocation[c])}}); const pieColors=['#3b82f6','#22c55e','#f59e0b','#ef4444']; return <div className="grid gap-4 lg:grid-cols-3"><div className="card p-5 lg:col-span-2"><h3 className="mb-4 font-semibold text-[#f5f5f5]">目标 vs 实际配置</h3><ResponsiveContainer width="100%" height={280}><BarChart data={data} barGap={6} barCategoryGap="28%"><CartesianGrid stroke="#222222" vertical={false}/><XAxis dataKey="category" tick={{fill:'#888888'}} axisLine={{stroke:'#222222'}} tickLine={false}/><YAxis tickFormatter={(v)=>formatPct(v)} tick={{fill:'#888888'}} axisLine={false} tickLine={false}/><Tooltip cursor={{fill:'rgba(255,255,255,.04)'}} contentStyle={{background:'#111111',border:'1px solid #222222',borderRadius:12,color:'#f5f5f5'}} formatter={(v)=>formatPct(v)}/><Legend wrapperStyle={{color:'#888888'}}/><Bar dataKey="目标" fill="rgba(59,130,246,.35)" radius={[6,6,0,0]}/><Bar dataKey="实际" fill="#3b82f6" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div><div className="card p-5"><h3 className="mb-4 font-semibold text-[#f5f5f5]">实际占比</h3><ResponsiveContainer width="100%" height={280}><PieChart><Pie data={data} dataKey="实际" nameKey="category" outerRadius={92} label={({category,实际})=>`${category} ${formatPct(实际)}`}>{data.map((d,i)=><Cell key={d.category} fill={pieColors[i%pieColors.length]}/>)}</Pie><Tooltip contentStyle={{background:'#111111',border:'1px solid #222222',borderRadius:12,color:'#f5f5f5'}} formatter={(v)=>formatPct(v)}/></PieChart></ResponsiveContainer></div></div> }
