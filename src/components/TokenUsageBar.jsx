import { estimateCost } from '../services/deepseek.js';
export default function TokenUsageBar({ usage }){ if(!usage) return null; const input=usage.prompt_tokens||usage.inputTokens||0; const output=usage.completion_tokens||usage.outputTokens||0; const reasoning=usage.completion_tokens_details?.reasoning_tokens||usage.reasoningTokens||0; return <div className="mt-2 rounded bg-[#1e1e1e] p-2 text-xs text-gray-400">本次消耗：Input {input.toLocaleString()} | Output {output.toLocaleString()} | Reasoning {reasoning.toLocaleString()} tokens | ≈${estimateCost(input,output).toFixed(4)}</div> }
