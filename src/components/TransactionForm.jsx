import { useEffect, useState } from 'react';
import { makeId, today, yuanToCents } from '../utils/formatters.js';
import { saveTransaction } from '../db/index.js';
export default function TransactionForm({ funds=[], editing, onSaved }){ const [form,setForm]=useState({date:today(),fundCode:'',type:'buy',shares:'',price:'',amount:'',fee:'0',notes:''}); const [error,setError]=useState(''); useEffect(()=>{ if(editing) setForm({...editing, amount:(editing.amountCents||0)/100, fee:(editing.feeCents||0)/100}); else if(funds[0]) setForm(f=>({...f,fundCode:f.fundCode||funds[0].code})); },[editing,funds]); const set=(k,v)=>setForm(f=>({...f,[k]:v, ...(k==='shares'||k==='price'?{amount:((k==='shares'?v:f.shares)*(k==='price'?v:f.price)||0).toFixed(2)}:{})})); async function submit(e){ e.preventDefault(); try{ if(!form.fundCode) throw new Error('请选择基金'); await saveTransaction({id:editing?.id||makeId(),date:form.date,fundCode:form.fundCode,type:form.type,shares:Number(form.shares),price:Number(form.price),amount:Number(form.amount),amountCents:yuanToCents(form.amount),fee:Number(form.fee),feeCents:yuanToCents(form.fee),notes:form.notes}); setError(''); onSaved?.(); }catch(err){ setError(err.message); } } return <form onSubmit={submit} className="card grid gap-3 p-4 md:grid-cols-4"><input className="input" type="date" value={form.date} onChange={e=>set('date',e.target.value)}/><select className="input" value={form.fundCode} onChange={e=>set('fundCode',e.target.value)}>{funds.map(f=><option key={f.code} value={f.code}>{f.code} {f.name}</option>)}</select><select className="input" value={form.type} onChange={e=>set('type',e.target.value)}><option value="buy">买入</option><option value="sell">卖出</option><option value="reinvest">红利再投</option></select><input className="input" placeholder="份额" value={form.shares} onChange={e=>set('shares',e.target.value)}/><input className="input" placeholder="成交净值" value={form.price} onChange={e=>set('price',e.target.value)}/><input className="input" placeholder="金额" value={form.amount} onChange={e=>set('amount',e.target.value)}/><input className="input" placeholder="手续费" value={form.fee} onChange={e=>set('fee',e.target.value)}/><input className="input" placeholder="备注" value={form.notes} onChange={e=>set('notes',e.target.value)}/>{error&&<div className="danger md:col-span-4">{error}</div>}<button className="btn md:col-span-4">保存交易</button></form> }
