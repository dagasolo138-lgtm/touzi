import { useEffect, useState } from 'react';
import HoldingsTable from '../components/HoldingsTable.jsx';
import NavRefreshButton from '../components/NavRefreshButton.jsx';
import { getFund, saveFund, saveNav, saveTransaction } from '../db/index.js';
import { fetchNav } from '../services/fundApi.js';
import { useStore } from '../store/useStore.js';
import { makeId, today, yuanToCents } from '../utils/formatters.js';
export default function Holdings(){ const {summary,loadAll}=useStore(); const [open,setOpen]=useState(false); const [step,setStep]=useState(1); const [error,setError]=useState(''); const [data,setData]=useState({code:'',category:'A股',hasHolding:true,date:today(),shares:'',nav:'',costPrice:'',notes:'期初持仓'}); useEffect(()=>{loadAll();},[loadAll]); async function query(){ try{ if(!/^\d{6}$/.test(data.code)) throw new Error('请输入6位基金代码'); if(await getFund(data.code)) throw new Error('该基金已在持仓中'); const r=await fetchNav(data.code); setData(d=>({...d,name:r.name,nav:r.nav,navDate:r.navDate})); setStep(2); setError(''); }catch(e){setError(e.message);} } async function confirm(){ try{ await saveFund({code:data.code,name:data.name,category:data.category,subCategory:data.subCategory||'',notes:data.fundNotes||''}); if(data.hasHolding){ const price=Number(data.costPrice||data.nav); const amount=Number(data.shares)*price; await saveTransaction({id:makeId(),type:'buy',date:data.date,fundCode:data.code,shares:Number(data.shares),price,amount,amountCents:yuanToCents(amount),fee:0,feeCents:0,notes:data.notes||'期初持仓'}); } await saveNav({fundCode:data.code,date:data.navDate||data.date,nav:Number(data.nav),source:'api'}); setOpen(false); setStep(1); await loadAll(); }catch(e){setError(e.message);} } return <div className="space-y-6"><div className="flex items-center justify-between"><h2 className="text-2xl font-bold text-white">持仓明细</h2><div className="flex gap-3"><button className="btn" onClick={()=>setOpen(true)}>+ 添加基金</button><NavRefreshButton/></div></div>{summary.holdings.length===0?<div className="card p-10 text-center text-gray-400">点击「添加基金」开始录入持仓</div>:<HoldingsTable holdings={summary.holdings}/>} {open&&<div className="fixed inset-0 z-10 grid place-items-center bg-black/60"><div className="card w-full max-w-xl p-6"><h3 className="mb-4 text-xl font-semibold">添加基金 · 第{step}步</h3>{step===1&&<div className="space-y-3"><input className="input w-full" placeholder="6位基金代码" value={data.code} onChange={e=>setData({...data,code:e.target.value})}/><button className="btn" onClick={query}>查询</button></div>}{step===2&&<div className="grid gap-3"><input className="input" readOnly value={`${data.code} ${data.name}`}/><select className="input" value={data.category} onChange={e=>setData({...data,category:e.target.value})}>{['A股','QDII','债券','黄金'].map(c=><option key={c}>{c}</option>)}</select><input className="input" placeholder="子类别" onChange={e=>setData({...data,subCategory:e.target.value})}/><textarea className="input" placeholder="备注" onChange={e=>setData({...data,fundNotes:e.target.value})}/><button className="btn" onClick={()=>setStep(3)}>下一步</button></div>}{step===3&&<div className="grid gap-3"><label><input type="checkbox" checked={data.hasHolding} onChange={e=>setData({...data,hasHolding:e.target.checked})}/> 有当前持仓</label>{data.hasHolding&&<><input className="input" type="date" value={data.date} onChange={e=>setData({...data,date:e.target.value})}/><input className="input" placeholder="持有份额" value={data.shares} onChange={e=>setData({...data,shares:e.target.value})}/><input className="input" placeholder="当前净值" value={data.nav} onChange={e=>setData({...data,nav:e.target.value})}/><input className="input" placeholder="持有成本均价（可选）" onChange={e=>setData({...data,costPrice:e.target.value})}/></>}<button className="btn" onClick={confirm}>确认</button></div>}{error&&<p className="danger mt-3">{error}</p>}<button className="btn2 mt-4" onClick={()=>setOpen(false)}>关闭</button></div></div>}</div> }
