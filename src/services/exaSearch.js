export function getApiKey(name) { return localStorage.getItem(`${name}ApiKey`) || ''; }
export async function exaSearch(query, type = 'auto') { try { const key = getApiKey('exa'); if (!key) throw new Error('请先配置 Exa API Key'); const res = await fetch('https://api.exa.ai/search', { method: 'POST', headers: { 'Content-Type': 'application/json', 'x-api-key': key }, body: JSON.stringify({ query, type: type === 'news' ? 'neural' : 'auto', numResults: 5 }) }); const data = await res.json(); if (!res.ok) throw new Error(data.message || 'Exa 搜索失败'); return data; } catch (error) { throw new Error(error.message || 'Exa 搜索失败'); } }
